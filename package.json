{
  "name": "focuslens",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev:desktop": "pnpm --filter @focuslens/desktop tauri dev",
    "build:extension": "pnpm --filter @focuslens/extension build"
  }
}
